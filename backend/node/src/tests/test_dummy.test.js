// This is a dummy test to ensure the CI pipeline is functional.
// Delete this file once real backend functionality is implemented.

describe('CI Dummy Test (Node backend)', () => {
  test('should pass', () => {
    expect(true).toBe(true);
  });
});
